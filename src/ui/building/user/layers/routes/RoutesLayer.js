import React from 'react';
import PropTypes from 'prop-types';

const RoutesLayer = (props) => {
    return (
        <div>
            This is routes layer
        </div>
    )
};

RoutesLayer.propTypes = {

};

export default RoutesLayer;